pipeline{
    agent any 
    stages{
        stage("check the connection k8s cluster"){
            steps{
            script{
                withCredentials([kubeconfigFile(credentialsId: 'kubernetes-config')]) {
                   sh ' kubectl get po '
                }
              }
            }
        }
