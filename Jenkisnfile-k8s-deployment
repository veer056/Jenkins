pipeline{
    agent any 
    stages{
        stage("check the connection k8s cluster"){
            steps{
            script{
                withCredentials([kubeconfigFile(credentialsId: 'mykubeconfig')]) {
                   sh ' kubectl get po -n devops '
                }
              }
            }
        }
        
         
